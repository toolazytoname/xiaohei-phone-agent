# 句子 TTS 队列

`SentenceTtsQueue` 将有界文本流切分为句子，并让首句立即可用。后续句子只会在当前代际完成后推进。

替换文本流或取消会清空队列并递增代际。旧代际的完成事件会被忽略，所以已取消的文本无法恢复或在之后进入播报。该队列没有 Android TTS、网络、音频焦点、麦克风或自动恢复能力。

在接入真实 TTS 适配器，并在设备上验证可听首句延迟和取消前，VOICE-010 保持 `VERIFY`。
