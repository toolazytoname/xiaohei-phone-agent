# 小黑交付证据矩阵

状态日期：2026-08-10。本页是证据索引，不是发布宣言。只要某里程碑的出口门禁缺少当前证据，就只能标记为“部分完成”。

| 里程碑 | 当前状态 | 已有证据 | 仍缺的出口门禁 |
|---|---|---|---|
| M0 — 契约与基线 | 已完成 | 意图、快照、轨迹、诊断、配置均有版本化契约；通用/模型/设备发布范围已双语冻结；仓库外 RSA-4096 签名治理与精确源码/证书/APK/SBOM 溯源包已生成。 | M0 契约无剩余门禁；公开上传和续航属于 M6。 |
| M1 — OnePlus 语音纵切 | 部分完成 | 三次冷启动、三次启停、三次息屏 DSP→本地 ASR→相册闭环，及事务式干净卸载/重装。 | 物理拔线 DSP OFF/ARMED 功耗 A/B；一次真实提示音听感检查。 |
| M2 — 日常短命令 | 已完成 | 12 个确定性动作、30 次回归、磁贴/状态通知停止/重启、独立隐私模型渠道；真实来电打断正在工作的本地 ASR，触发临时焦点丢失、释放麦克风、回到 `ARMED`，事后录音客户端为 0。 | M2 契约没有剩余门禁。 |
| M3 — 通用 Android Alpha | 已完成 | 源码基础 APK 不要求 DSP/root；干净 Android 14 AOSP ARM64 虚拟设备已完成全新安装、无权限 onboarding、麦克风拒绝、离线相册路由和卸载残留检查。 | M3 通用基础契约没有剩余门禁；CPU 唤醒功耗仍是 M7 条件。 |
| M4 — 通知与确认草稿 | 已完成 | 可选授权、锁屏/息屏拒绝、PUBLIC/PRIVATE/SECRET、精确通知新鲜性、运行中撤权清除、易失草稿，以及独立 AOSP Messaging 合成身份；确认只打开 App，发送箱仍为 0，小黑不持久化消息数据。 | M4 契约没有剩余门禁；无人值守真实微信主账号自动化明确不在范围内。 |
| M5 — 可见 Phone Agent | 已完成 | 预览后执行、本地策略、动作前后包绑定、全局停止、设置两步、计算器任务、10/10 个不同 AOSP App 矩阵、有界脱敏轨迹，以及真实通知触发的本机视觉恢复和人工精确标签重试；截图不上传、不落盘。 | M5 契约没有剩余门禁。 |
| M6 — 公开 Beta 加固 | 部分完成 | debug/release 分离、仓库外 release 身份、精确 APK 扫描/SBOM/溯源、精确 release 全新安装回滚、升级降级卸载、100 次压力、25 页面、无 Token 备份恢复、字节一致构建、独立 ClamAV 扫描及已解密验证的签名恢复暂存。 | 8–24 小时物理拔线待机/功耗、把签名恢复包移到独立控制的离线介质，以及最终公开上传批准。模型权利只阻断私有含模型包，不阻断无模型通用候选。 |
| M7 — “小黑小黑”与多设备 | 部分完成 | 可选 CPU KWS 真机声学闭环到相册、与 DSP 独立控制、干净 Android 14 ARM64 通用 profile 的启停/卸载回滚、明确不宣称自定义 DSP，以及可复现的 80 用例合成诊断。 | 真人多人/噪声/距离与拔线功耗证据；只有两个标准普通话 TTS 音色具备可懂正样本对照，不能用合成诊断代替真人准确率验收。 |

## 长期增量能力

| 任务 | 当前状态 | 已有证据 | 仍缺的门禁 |
|---|---|---|---|
| `CHAT-003` — 有界 Conversation 传输 | 已完成任务范围 | SSE 优先、JSON 回退、loopback `NO_PROXY`、无重定向/重试、64 KiB 上限；11 条确定性传输用例、APK 构建和仓库门禁通过，零模型调用。 | `CHAT-005` 真人语音/TTS 与 `CHAT-012` 最终对话验收仍未完成。 |
| `CHAT-004` — 单轮 Conversation 界面 | 已完成任务范围 | 零动作权限双语界面、竞态安全的取消/销毁、六个稳定无障碍标识、动作边界静态门禁，以及一次全新 AOSP 模拟器用户路径流式 mock 回复；无崩溃或 ANR。 | 实体机凭据验收仍属于 `CHAT-002`；真人语音/TTS 和最终对话验收仍属于 `CHAT-005`/`CHAT-012`。 |
| `CHAT-002` — Keystore 与无 Token 备份 | OnePlus 8T 已通过 | 随机 Conversation 测试 Token 仅以 IV/密文进入 Android Keystore 流程；v3 系统分享预览不暴露 Token 材料；正常恢复可见清除三个槽并关闭三个渠道且不启动服务；原有本地离线状态已恢复。 | 真实远端凭据/请求、外部接收方、卸载/重装 Keystore 生命周期及其他 OEM 行为不属于本项。 |
| `ROOT-010` — 独立生命周期 | 独立 Android 14 AOSP ARM64 已通过 | 源码型调试包完成安装、升级、预期的普通降级拒绝、显式维护回退和事务卸载；无主包、Assistant role、DSP Companion 或 active recorder 残留。 | 这只是生命周期前置条件；没有执行 root adapter、root 命令、profile 事务或 root capability。 |
| `REL-005` — 通用恢复生命周期 | 独立 AOSP 证据后为 `VERIFY` | 带序列号保护的 Android 14 ARM64 AOSP 脚本已通过显式安装、强制结束、冷启动/强制结束、以 boot ID 确认的重启和卸载。每个终态均要求无小黑 PID、Android `ServiceRecord` 或 AudioFlinger 包名残留。 | 未覆盖网络降级、真实中转/模型超时、实体进程杀死、OnePlus DSP 重 arm 或人类可见恢复。 |
| `CHAT-006` — 有边界的纯内存会话 | 已完成任务范围 | 契约一致的 1–8 turn、64–8192 估算 token、1 秒–15 分钟边界；12 条确定性生命周期用例与无持久化/日志静态门禁通过。终止路径释放会话自身正文引用，新实例不恢复文字。 | 已由 `CHAT-007` 接入；本行仍只保证引用释放，不宣称 JVM 物理清零。 |
| `CHAT-007` — 半双工追问 | 已完成任务范围 | 6 轮/2048 token/5 分钟 UI 接入；11 条指代/结束/超时/profile/锁屏/后台/半双工/无效回复确定性用例；全新 AOSP 用户路径发送两轮流式请求，服务端验证精确历史，点击结束后 UI 清空，且无 Fatal/ANR。 | 幂等零调用对话控制仍属 `CHAT-009`，语音/TTS 与真人验收属 `CHAT-005`/`CHAT-012`。 |
| `CHAT-008` — 最小 Prompt 边界 | 已完成任务范围 | 唯一版本化静态 system Envelope；20 条 Prompt 注入、10 条助手工具伪造、5 种敏感形态、非法边界、不可变性及无动态上下文/无动作路径静态门禁均零模型调用通过。 | 这证明角色/上下文强制与零动作权限，不证明所有模型必然服从或回答质量。 |
| `CHAT-009` — 零调用本地控制 | 已完成任务范围 | 23 条精确短语解析、五控制幂等状态、9 组确定性用例、11 个稳定 UI 标识及全新 AOSP 按钮路径：一次基线 mock 后依次重说/停止/继续/清空，服务端计数仍为 1 且 UI 正文清空。 | 真人声学识别和朗读式重说仍属 `VOICE-*`/`CHAT-005`；本行只证明控制文字/按钮、状态和零模型调用。 |
| `CHAT-010` — 独立 TTS 选择器 | 已完成任务范围 | Off/System/Relay 配置隔离、独立 Keystore 槽、v3 无 Token 备份与 v2 恢复兼容、单元/静态/构建门禁；全新 AOSP Relay → System 用户路径保持 Conversation/Phone Agent 六字段不变，零 TTS 初始化、服务启动、Fatal/ANR。 | 本任务未实现 Relay 播放；小黑适配器播报/停止、真人可懂度、打断和完整语音轮次仍属 `VOICE-002/011`、`CHAT-005`、`CHAT-012`。 |
| `VOICE-001` — 离线中文系统 TTS 资格 | OnePlus 8T 已通过 | 所有者授权的 `ChineseTtsTflite 0.5.0` APK 与 F-Droid 签名索引 SHA-256 一致，注册 Android TTS 服务并被选为默认。小黑独立只读探针返回 `READY`、4 个中文音色全部离线、简体可用；内置 FastSpeech2 示例交付 81,600 帧，且没有引擎唤醒锁引用。 | 这证明安装、注册、离线元数据、初始化和本地示例输出；真人可懂度、小黑适配器停止时延和完整语音轮次仍属于 `VOICE-002/011`、`CHAT-005`、`CHAT-012`。 |
| `VOICE-002` — 离线系统 TTS 适配器 | OnePlus 8T 已通过 | 小黑 Conversation 初始化所选离线引擎，播报零模型调用的确定性 FAQ；点击停止播报后可见状态从 `SPEAKING` 变为 `INTERRUPTED`。Android 在 6,976 帧后停止音轨，Active Record Client 与小黑/引擎唤醒锁引用均为 0。原子生命周期和 utterance 失效拒绝迟到完成/错误回调，单测与签名构建通过。 | 证明真实适配器初始化、输出、显式中断及停止后资源状态；不证明真人听感、小于等于 300 ms 可听中断、完整语音轮次、长期功耗或 DSP ARMED 状态。 |
| `VOICE-004` — Android 音频半双工 | OnePlus 8T 已通过 | 绑定身份的进程 lease 接入 TTS、本地/系统 ASR 及可选 CPU KWS。真机 TTS 有一条 24 kHz Active Output、无 Recorder；离线 ASR 有一条 16 kHz Active Input、无 TTS；停止后双方归零。启动前取消竞态已复现、修复并重跑为 `capture_start_cancelled` 且不打开录音器。 | 证明小黑已接线路径的进程内排他与终态释放；跨 App 排他、真人音频、可听时延、路由、功耗和公开模型权利仍未完成。 |
| `VOICE-005` — 音频中断策略 | OnePlus 部分证据 | 静态策略覆盖来电/闹钟/媒体/Activity，绝不自动恢复音频。真实本地 ASR 录音启动后经 Activity pause 进入后台，记录麦克风释放；停止后 AudioFlinger 无 Active Record Client，DSP 仍为 active。 | 真实来电、闹钟、媒体焦点运行；输出路径/路由、无关 App 和真人音频仍未完成。 |
| `VOICE-010` — 句子 TTS 队列 | OnePlus 8T 已通过 | 带代际的有序队列、显式迟到回调失效、仅元数据的队列日志、单测/静态/签名构建门禁均通过；真实固定 FAQ 运行依序创建第 1–4 句的 24 kHz 音轨，第二次在第 2 句时停止，记录 `dropped=3`，之后无后续序号提交。 | 真人可懂度/自然度、≤300 ms 可听打断、抢话/回声、路由和长时间功耗仍属于 `VOICE-011/012` 与真人/物理门禁。 |
| `VOICE-011` — TTS 中断边界 | 自动化/设备边界已通过；真人门禁开放 | 可见停止播报会在停止引擎前使 utterance/队列失效；OnePlus 第 2 句音轨在用户控制事件后 227 ms 移除，记录 `dropped=3`，且不再提交后续队列。`VOICE-004` 提供进程内输入/输出排他。 | 音轨计时不等于人耳听感。仍需真人确认中文可懂度/自然度、≤300 ms 可听打断和可接受的真实声学回声环结果，之后才能讨论语音抢话。 |
| `CHAT-011` — 确定性离线 FAQ | 已完成任务范围 | 五类/25 条精确短语，10 条未知/动作/注入及超长拒绝；固定双语非模型标签；零新增模型/动作/上下文使用；远端失败后接入及未启用渠道零网络线程静态门禁；全新 AOSP 命中与动作型未知拒绝均留在 Conversation，零 Fatal/ANR。 | 未内置 0.6B 权重或生成式本地模型；更广离线推理、语音与动作属于独立后续门禁。 |
| `CHAT-012` — 最终 Conversation 验收 | 自动范围通过；真人门禁未完成 | 精确 20 问、5 中断、5 超时、5 本地隐私拒绝；五类/15 短语请求前隐私策略；静态零录音 API 路径、签名 APK；全新 AOSP 拒绝保持在 Conversation，零 Fatal/ANR、零 Active Record Client。 | `VERIFY`：尚无已实现 Relay 播放或真人中文可懂度/自然打断验收，自动化不能关闭该门禁。 |
| `ROUTE-002` — 安全三路交接 | AOSP 用户路径已通过 | 已审核 `CommandRouter` 加祈使线索；精确 100 条合成矩阵（40/35/25）；概念关键词、双目标歧义、多步骤和高风险回归；静态/构建门禁通过。在全新 Android 14 ARM64 AOSP 中，聊天文本只打开带可编辑预填和零模型/动作状态的 Conversation；复杂文本只打开带可编辑预填且未规划/执行的 Phone Agent；两条路径都不自动发送。 | 该交接没有实现远端规划器、工具执行、通用 Agent 或用户确认；它们仍是独立门禁。 |
| `ROUTE-003` — 零副作用澄清 | AOSP 用户路径已通过 | 纯本地目标/意图/范围决策；精确 50 条合成矩阵含 30 条追问和 20 条明确控制；每个追问均为聊天加未知命令，零猜测动作、模型或动作调用。全新 AOSP 中歧义文本留在主页，显示“需要澄清；没有执行”。 | 未声称真人声学资格或后续轮次消歧。 |
| `ROUTE-004` — 未确认 ActionRequest | 已完成任务范围；可见草稿交接已通过 | 仅接受有类型用户消息的 schema-v1 工厂；不可变高风险 pending dry-run 请求；39 条合成矩阵含 10 条助手确认伪造；4 个公开 Schema 夹具；零模型/动作调用；矛盾 pending/live 与 pending/免确认载荷拒绝。复杂路由只预填可编辑 Phone Agent 文本，规划与确认仍是后续用户手势。 | 真实规划器、确认到工具的 bridge 以及可执行的复杂计划仍是独立门禁。 |
| `PLAN-001` — 规则优先 Task Plan v1 | 已完成任务范围 | 绑定请求、1–8 步/1–60 秒 dry-run DAG；步骤/依赖/参数视图不可修改；精确 34 条 Java 矩阵和 5 个公开夹具；当前七工具目录/风险强制；前向 DAG 合法，未知工具、超预算、非法字段与 5 个真实循环均零模型/动作调用拒绝。 | 有意尚未接入 UI/模型/执行器；确认、目录、授权及有界 Android bridge 已存在，但 `PLAN-002` 最小远端适配与设备证据仍是独立门禁。 |
| `POLICY-002` — 新鲜范围绑定确认 | 已完成任务范围 | 仅本机用户、前台解锁、纯内存一次性票据；绑定 task/request/plan 与加盐目标/内容摘要；1–60 秒单调时钟过期；精确 50 条 Java 矩阵和 4 个公开夹具；目标/内容/身份变化、过期/回退、锁屏/后台、助手来源、取消与重放均零模型/动作调用关闭。唯一可见消费者是 `TOOL-003` 默认取消的只读测试集合查询。 | 它仍不向模型、远端调用方、任意工具或通用执行器开放；完整提案预览仍需 `UX-004`。 |
| `TOOL-001` — 版本化工具目录 | 已完成任务范围 | 不可变七工具 v1 Android 目录；精确风险、十个真实封闭输入/输出 Schema、同目录回滚目标、受众及 100–60000 ms 超时元数据；Java 测试和五个公开 fixture 拒绝重名、未知版本、缺失 Schema、无法解析回滚、未知工具与风险不匹配。静态门禁确认零模型/平台/执行/持久化路径，且无新增 UI 接线。 | 目录成员资格不授予权限，回滚仍只是声明。`TOOL-002` 提供授权，`TOOL-003` 提供有界 Android bridge；可见确认和设备验收仍在用户可见执行之前。OpenCode/root 保持独立层级。 |
| `TOOL-002` — loopback capability 授权 | 已完成任务范围 | 一次成功的内部确认 receipt 只能换取一次纯内存 Token；默认 `SecureRandom` 128-bit、1–30 秒，绑定 confirmation/task/request/plan/call/工具/版本/风险/受众/参数/幂等/时间/隐私摘要。精确 50 组 Java 与 7 个公开 fixture 文件拒绝 10 个远端/跨 UID peer、确认复用/错范围、7 种调用漂移、3 种目录漂移、5 种非法/未来/陈旧/私有元数据调用、过期/时钟回退、重放/撤销与跨网关。活动/重放状态有界，模型/动作/执行调用均为 0；本地 `TOOL-003` 页面是唯一可见消费者。 | 没有 socket 监听器或远端 API；后续可信传输层必须自己产生 peer 证据，不能接受请求 JSON。 |
| `TOOL-003` — 有界执行生命周期 | AOSP 只读路径已通过；真实适配器门禁未完成 | 调用超时受审核目录上限约束并写入 capability 摘要；私有许可只消费一次；输出不可变、有界且私有。精确 25 组 Java 覆盖 5 次注入成功、5 次中断式到期、5 个取消路径、5 个类型化失败、5 个拒绝/重放路径、9 次 worker 中断确认及零或一次适配器调用。封闭 Android 注册表和受权 bridge 只解析受审的 MediaStore 测试/日历测试适配器；全局停止提供协调器取消信号。在全新 Android 14 ARM64 AOSP 模拟器中，用户从主页进入内部页，准备步骤零执行；明确本机确认返回真实 MediaStore 测试集合计数 `0`，随后第二个待确认请求被取消；调试 APK 已卸载、模拟器已停止。 | `VERIFY`：只覆盖安全的只读查询。可变适配器的权限/回滚、可信外部 peer、真实网络/进程 kill、实体机和资源关闭证据仍未完成。 |
| `OC-002` — OpenCode 任务协议 | 已完成任务范围 | 封闭私有 `opencode-task.v1` 提案只接受当前用户、pending、dry-run 的复杂任务，并且仅有项目总结、测试诊断、受控文件整理三类审核类型。精确 Java 覆盖 3 个合法类型、10 条保持惰性的指令形攻击、6 条来源/类型/身份拒绝、不可修改的脱敏元数据和零模型/动作/执行调用；2 个合法/3 个拒绝公开 fixture 与无 UI/无平台/无通用 shell 静态门禁通过。 | 这不是 OpenCode runner：不会创建工作区、进程、Prompt、网络连接、文件访问或用户可见确认。`OC-003` 必须先提供任务范围路径及穿越/符号链接/跨任务拒绝，runner 才能出现。 |
| `OC-003` — 任务私有工作区边界 | 已完成任务范围 | 合法 pending OpenCode 任务会在固定 App 私有根目录下获得空的 `input`/`output` 根。真实临时文件系统矩阵证明 2 份租约、4 条安全相对路径、7 条绝对/穿越拒绝、3 条符号链接拒绝、2 条跨任务拒绝、重复任务拒绝及零内容读取/写入/进程调用；无路径公开 lease fixture 与无 runner/UI 静态门禁通过。 | 它只是解析器：没有 OpenCode runner、文件内容打开、原子安全打开、用户可见确认或清理生命周期。`OC-004` 与 `OC-006` 必须在不重新开放路径权限的前提下补有界执行和清理。 |
| `OC-004` — 有界 OpenCode runner | 已完成任务范围 | 注入适配器只获得合法 pending 任务、匹配私有租约、有界计量器和取消信号。runner 强制审核 profile/agent 标记、100–60,000 ms、1–4,096 token、1–32 步及 1–4,096 代码点脱敏输出。Java 覆盖 4 次成功、3 次上限越界、5 次拒绝、1 次超时和 2 次取消；公开预算 fixture 与进程/网络/内容/UI 静态门禁通过。 | 它不是实际 `oc run`：不启动进程、不连接模型/网络；尚无适配器证明完整计量、安全打开、清理或真实设备取消。 |
| `OC-005` — 脱敏可见进度 | 已完成任务范围 | 6 个类型化生命周期事件映射到可安全公开日志的 Agent 只读卡，只含任务类别、生命周期状态和有界已完成步骤数。矩阵覆盖未连接、7 次生命周期转换、终态幂等和 4 类敏感字段排除；Android 构建及 UI/脱敏静态门禁通过。 | 当前没有 live OpenCode 适配器，因此默认卡显示未连接；它不是 OpenCode 已运行的证据。真实 runner 接线前，`OC-006` 必须提供停止/清理。 |
| `OC-006` — 停止与清理 | 已完成任务范围 | 已登记任务停止会取消 worker、撤销活跃本地网关 Token、调用注入的 process/listener/tmux 资源句柄，并无跟随递归删除唯一已登记私有租约。临时文件系统矩阵覆盖 3 个资源、错误任务拒绝、重复停止幂等、递归清理和链接外部目标保留。 | 此处不会启动真实 OS 进程、端口、监听器或 tmux 句柄。实际适配器必须终止自身资源并通过独立设备验收。 |
| `OC-007` — 受限工具 | 已完成任务范围 | 封闭、零执行策略只允许项目摘要、测试诊断和受控文件整理。对抗矩阵允许 3 条审核意图，拒绝 root、敏感路径、破坏性 Git/删除、网络、shell 转义和未知文本。 | 它是意图边界，不是 shell 解析器或命令执行器。未来适配器只能接受批准的类型操作，并保留设备验收门禁。 |
| `OC-008` — 受控任务矩阵 | 已完成任务范围 | 9 轮合成临时工作区回归组合了类型任务协议、受限策略、注入有界适配器、结构化成功结果和私有清理：摘要、诊断、整理各 3 轮；每类还拒绝 Git/网络对抗意图。 | `real_opencode=0`：没有打开用户项目、实际模型、OpenCode 进程、网络或真实工具；仍需真实适配器/设备验收。 |

## 证据规则

- 源码修改、单元测试和静态扫描只证明实现，不能替代环境或用户可见的验收。
- 夹具、mock、同机 profile-disabled 运行必须如实标注，不能满足独立账号/设备门禁。
- 同一失败指纹只记录一次；先改变条件或修复再复测。没有新条件时，重复截图、声学提示或付费模型调用不是新证据。
- debug 或测试签名 APK 只能作为内部验收工件，不能作为公开 Release 宣传。

## 权威记录

- [M1 OnePlus 记录](acceptance-oneplus8t-m1.zh-CN.md)
- [M2 短命令](acceptance-m2.zh-CN.md)
- [M3 通用 Android](acceptance-m3.zh-CN.md)
- [M4 通知](acceptance-m4.zh-CN.md)
- [M5 Phone Agent](acceptance-m5.zh-CN.md)
- [M6 加固](acceptance-m6.zh-CN.md)
- [M7 唤醒词](acceptance-m7.zh-CN.md)
- [CHAT-002 Keystore 与无 Token 备份](acceptance-chat-002.zh-CN.md)
- [ROOT-010 独立生命周期](acceptance-root-010.zh-CN.md)
- [REL-005 通用生命周期恢复](acceptance-rel-005.zh-CN.md)
- [VOICE-001 离线中文 TTS](acceptance-voice-001.zh-CN.md)
- [VOICE-002 离线系统 TTS 适配器](acceptance-voice-002.zh-CN.md)
- [VOICE-004 Android 音频半双工](acceptance-voice-004.zh-CN.md)
- [VOICE-005 音频中断](acceptance-voice-005.zh-CN.md)
- [VOICE-010 句子 TTS 队列](acceptance-voice-010.zh-CN.md)
- [VOICE-011 TTS 中断边界](acceptance-voice-011.zh-CN.md)
- [公开发布清单](release-checklist.zh-CN.md)
