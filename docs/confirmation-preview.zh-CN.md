# 确认预览

在确认受限 Phone Agent 提议前，界面会固定展示一张核对卡：App、目标、拟执行内容、所需权限和停止/回滚路径。取消是默认安全决定；没有倒计时确认，也没有预选同意。

预览只描述现有的单步 Phone Agent 路径。它不会签发 `FreshConfirmationGate` 回执、申请或扩大 Android 权限、调用模型、启动无障碍任务，也不会调用工具、OpenCode、root 或适配器。既有确认边界仍要求本机前台用户手势。

对此路径，回滚表示使用 **全局停止 Phone Agent**。被停止或失败的任务不会自动重试或继续。
