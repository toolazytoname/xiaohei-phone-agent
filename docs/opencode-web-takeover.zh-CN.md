# OpenCode Web 接管所有权

`OpenCodeTakeoverOwnership` 建模本地执行器与已验证 Web 会话之间的显式控制权。接管和归还只转移控制权，不会启动、恢复、复制或改变任务。

只有不透明、已在本地验证的 `web-…` 会话句柄可以请求转移。重复接管、错误归还或终态后的请求都会被拒绝。该核心不实现服务器、Web UI、传输、任务正文、文件系统访问或进程执行。

在真实本地 Web 交接接线、可见用户控制以及独立证明不会复制在途任务之前，OC-009 保持 `VERIFY`。
