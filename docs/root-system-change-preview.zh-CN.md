# Root 系统修改预览

[English](root-system-change-preview.md) · [profile 事务](root-profile-transaction.zh-CN.md)

`ROOT-007` 只接受固定 profile 目标、不同的 SHA-256 前后摘要、新鲜确认且未过期的请求。它只返回固定摘要 `profile-digest-change`，目标和摘要不会进入结果。没有 apply、root、shell、文件或设备路径。
