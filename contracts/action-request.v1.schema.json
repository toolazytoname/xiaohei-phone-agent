{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/toolazytoname/xiaohei-phone-agent/contracts/action-request.v1.schema.json",
  "title": "Xiaohei Action Request v1",
  "description": "A locally constructed action lifecycle record. New complex-task proposals enter as confirmation_state=pending and dry_run=true; this schema does not grant execution authority.",
  "type": "object",
  "required": [
    "schema_version",
    "request_id",
    "target",
    "action",
    "risk",
    "requires_confirmation",
    "confirmation_state",
    "dry_run",
    "created_at",
    "redaction"
  ],
  "properties": {
    "schema_version": {
      "const": 1
    },
    "request_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{7,127}$"
    },
    "target": {
      "enum": ["android_app", "android_notification", "android_accessibility", "local_service"]
    },
    "action": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "parameters": {
      "type": "object"
    },
    "risk": {
      "enum": ["low", "medium", "high"]
    },
    "requires_confirmation": {
      "type": "boolean"
    },
    "confirmation_state": {
      "enum": ["not_required", "pending", "confirmed", "rejected", "expired"]
    },
    "dry_run": {
      "type": "boolean"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "redaction": {
      "type": "object",
      "required": ["public_log_safe"],
      "properties": {
        "public_log_safe": {
          "type": "boolean"
        },
        "sensitive_fields": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "risk": {
            "const": "high"
          }
        },
        "required": ["risk"]
      },
      "then": {
        "properties": {
          "requires_confirmation": {
            "const": true
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "confirmation_state": {
            "const": "pending"
          }
        },
        "required": ["confirmation_state"]
      },
      "then": {
        "properties": {
          "requires_confirmation": {
            "const": true
          },
          "dry_run": {
            "const": true
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "confirmation_state": {
            "const": "not_required"
          }
        },
        "required": ["confirmation_state"]
      },
      "then": {
        "properties": {
          "requires_confirmation": {
            "const": false
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "requires_confirmation": {
            "const": false
          }
        },
        "required": ["requires_confirmation"]
      },
      "then": {
        "properties": {
          "confirmation_state": {
            "const": "not_required"
          }
        }
      }
    }
  ],
  "additionalProperties": false
}
