# 证据变化恢复

`FailureFingerprint.RecoveryGate` 是针对单次任务尝试的内存 fail-closed 恢复策略。它记录第一个有界失败指纹，拒绝相同的失败或恢复请求；只有新的指纹证明观察条件已改变时，才精确放行一次恢复。

放行后，所有后续恢复请求都会被拒绝，即使证据再次变化也一样。无效或缺失的证据同样被拒绝。该守卫不执行工具、不重试请求、不持久化指纹，也不公开指纹。

PLAN-004 保持 `VERIFY`：未来规划器/执行器必须把它接到真实观察，并在任何真实适配器依赖它前向用户展示唯一的恢复路径。
