# Failure recovery card

The public task-card failure view accepts only a fixed failure kind. It shows a bilingual cause, impact, and one manual recovery path; it never displays exception text, paths, prompts, model output, or credentials. Current adoption is task-card-only, so `UX-008` remains VERIFY until other execution surfaces adopt it.
