# Policy Engine

Turns recognized text into a structured, risk-scored, confirmation-aware action request. Model output is untrusted input until policy validation succeeds.

把识别文本转成结构化、带风险等级和确认状态的动作请求。模型输出在通过策略校验前始终是不可信输入。
