# Delivery evidence matrix

Status date: 2026-08-10. This is an evidence index, not a release claim. A row is `partial` whenever any stated exit gate lacks current evidence.

| Milestone | Current status | Evidence that exists | Exit gates still missing |
|---|---|---|---|
| M0 — contracts and baseline | complete | Versioned intent, snapshot, trace, diagnostic, and configuration contracts; frozen bilingual generic/model/device release scope; external RSA-4096 signing governance; exact source/certificate/APK/SBOM provenance bundle. | None for the M0 contract. Public upload and endurance gates remain M6 work. |
| M1 — OnePlus voice vertical slice | partial | Three cold boots, three arm/disarm cycles, three screen-off DSP → local ASR → gallery chains, clean transactional uninstall/reinstall. | Physically unplugged DSP OFF/ARMED power A/B; one audible-prompt listening check. |
| M2 — daily short commands | complete | 12 deterministic actions, 30-action regression, tile/status-stop/reboot, privacy-preserving independent model channels; a real incoming call interrupted an active local-ASR session, produced transient focus loss, released the microphone, returned to `ARMED`, and left zero recording clients. | None for the M2 contract. |
| M3 — generic Android Alpha | complete | Source-only base APK has no DSP/root requirement; a clean Android 14 AOSP ARM64 virtual device completed fresh install, no-permission onboarding, microphone denial, offline gallery routing, and uninstall residue checks. | None for the M3 generic-base contract. CPU wakeword power remains an M7 condition. |
| M4 — notifications and confirmed drafts | complete | Optional access, lock/screen-off denial, PUBLIC/PRIVATE/SECRET handling, exact-notification freshness, dynamic revocation clearing, volatile visible draft, and an independent AOSP Messaging synthetic identity. Confirmation opened only the app; sent rows remained zero and no message data persisted in Xiaohei. | None for the M4 contract; unattended primary-WeChat automation remains explicitly out of scope. |
| M5 — visible Phone Agent | complete | Preview-before-execute, local policy, package binding before/after actions, global stop, Settings two-step and Calculator tasks, a 10/10 distinct-app AOSP matrix, bounded redacted trace, and a real notification-invoked local visual recovery followed by manual exact-label retry. Screenshots are never uploaded or persisted. | None for the M5 contract. |
| M6 — public Beta hardening | partial | Debug/release separation, external release identity, exact APK scan/SBOM/provenance, exact release fresh-install rollback, upgrade/downgrade/uninstall, 100-action stress, 25 surfaces, tested non-secret backup/restore, byte-identical builds, independent ClamAV scan, and verified encrypted signing-recovery staging. | 8–24 hour physical unplugged idle/power, moving the verified signing recovery to separately controlled offline media, and final public-upload approval. Model rights block only the private model-bearing package, not the no-model generic candidate. |
| M7 — “Xiaohei Xiaohei” and more devices | partial | Opt-in CPU KWS acoustic end-to-end to gallery, independent DSP control, a clean Android 14 ARM64 generic profile with start/stop/uninstall rollback, a narrowed no-custom-DSP claim, and a reproducible 80-case synthetic diagnostic. | Real multi-speaker/noise/distance and unplugged-power evidence; only two standard Mandarin TTS voices have intelligible positive controls, so the synthetic diagnostic cannot qualify human accuracy. |

## Long-term incremental capabilities

| Task | Current state | Evidence now | Remaining gates |
|---|---|---|---|
| `CHAT-003` — bounded Conversation transport | Complete in task scope | SSE-first with JSON fallback, loopback `NO_PROXY`, no redirects/retries, and a 64 KiB cap; eleven deterministic transport cases, APK build, and repository gates pass with zero model calls. | The `CHAT-005` human voice/TTS loop and final `CHAT-012` conversation acceptance remain open. |
| `CHAT-004` — single-turn Conversation UI | Complete in task scope | Zero-authority bilingual UI, race-safe cancel/destroy handling, six stable accessibility identifiers, static action-boundary gate, and one fresh AOSP-emulator user-path streaming mock reply without crash or ANR. | Physical-device credential acceptance remains `CHAT-002`; human speech/TTS and final conversation acceptance remain `CHAT-005`/`CHAT-012`. |
| `CHAT-002` — Keystore and token-free backup | Passed on OnePlus 8T | A random Conversation test token was stored as only IV/ciphertext under the Android Keystore flow; v3 system-share preview exposed no token material; normal restore visibly cleared all three slots and disabled the three channels without starting services; original local offline state was restored. | Real remote credential/request, external recipient, uninstall/reinstall Keystore lifecycle, and other OEM behavior remain outside this task. |
| `ROOT-010` — independent lifecycle | Passed on independent Android 14 AOSP ARM64 | Source-only debug packages completed install, upgrade, expected ordinary downgrade rejection, explicit maintenance rollback, and transactional uninstall; no main package, Assistant role, DSP Companion, or active recorder remained. | This is a lifecycle prerequisite only. No root adapter, root command, profile transaction, or root capability was executed. |
| `REL-005` — generic recovery lifecycle | `VERIFY` after independent AOSP evidence | A serial-guarded Android 14 ARM64 AOSP harness passed explicit install, force-stop, cold-start/force-stop, boot-ID-verified reboot, and uninstall. Every terminal phase required no Xiaohei PID, Android `ServiceRecord`, or AudioFlinger package residue. | Network degradation, a real relay/model timeout, physical process kill, OnePlus DSP re-arm, and human-visible recovery are not exercised. |
| `CHAT-006` — bounded in-memory session | Complete in task scope | Contract-aligned 1–8 turn, 64–8192 estimated-token, and 1 s–15 min bounds; twelve deterministic lifecycle cases and a static no-persistence/logging gate pass. Terminal paths release session-owned transcript references; new instances restore no text. | Integrated by `CHAT-007`; this remains a reference-release guarantee, not a JVM memory-zeroization claim. |
| `CHAT-007` — half-duplex follow-up | Complete in task scope | Six-turn/2048-token/five-minute UI integration; eleven deterministic reference/end/timeout/profile/lock/background/half-duplex/invalid-reply cases; fresh AOSP user path sent two streaming turns, server verified exact prior context, UI cleared on End, and no fatal/ANR occurred. | Idempotent zero-call conversation controls remain `CHAT-009`; speech/TTS and human acceptance remain `CHAT-005`/`CHAT-012`. |
| `CHAT-008` — minimal prompt boundary | Complete in task scope | One versioned static system envelope; 20 prompt injections, 10 assistant tool forgeries, five sensitive shapes, malformed bounds, immutability, and static no-dynamic-context/no-action-path gates pass with zero model calls. | This proves role/context enforcement and zero action authority, not universal model obedience or answer quality. |
| `CHAT-009` — zero-call local controls | Complete in task scope | Exact 23-phrase parser, five-control idempotent state, nine deterministic groups, eleven stable UI identifiers, and a fresh AOSP button path: one baseline mock call followed by Repeat/Stop/Continue/Clear while server count stayed one and UI text cleared. | Acoustic recognition and spoken Repeat remain under `VOICE-*`/`CHAT-005`; this row proves control text/buttons, state, and zero model calls. |
| `CHAT-010` — independent TTS selector | Complete in task scope | Off/System/Relay configuration isolation; dedicated Keystore slot; token-free v3 backup with v2 restore; unit/static/build gates; fresh AOSP Relay → System user path preserved all six Conversation/Phone Agent fields with zero TTS initialization, service start, fatal, or ANR. | Relay playback is not implemented here. Xiaohei adapter speech/stop, human intelligibility, interruption, and full speech turns remain `VOICE-002/011`, `CHAT-005`, and `CHAT-012`. |
| `VOICE-001` — offline Chinese system TTS qualification | Passed on OnePlus 8T | Owner-authorized `ChineseTtsTflite 0.5.0` APK matched the signed F-Droid index SHA-256, registered an Android TTS service, and became the selected default. Xiaohei's independent read-only probe returned `READY`, 4 Chinese voices/4 offline, and Simplified Chinese available; the built-in FastSpeech2 sample delivered 81,600 frames with no engine wake-lock reference. | This proves installation, registration, offline metadata, initialization, and local sample output. Human intelligibility, Xiaohei adapter stop latency, and complete speech turns remain `VOICE-002/011`, `CHAT-005`, and `CHAT-012`. |
| `VOICE-002` — offline system-TTS adapter | Passed on OnePlus 8T | Xiaohei Conversation initialized the selected offline engine, spoke a deterministic zero-model FAQ, and visibly changed `SPEAKING` to `INTERRUPTED` on Stop Speech. Android stopped the track after 6,976 frames; active record clients and Xiaohei/engine wake-lock references were zero. Atomic lifecycle and utterance invalidation reject stale completion/error callbacks; unit and signed-build gates pass. | Proves real adapter initialization, output, explicit interruption, and post-stop resource state. It does not prove human sound quality, ≤300 ms audible interruption, complete voice turns, long-duration power, or DSP ARMED state. |
| `VOICE-004` — Android audio half-duplex | Passed on OnePlus 8T | Identity-bound process leases wire TTS, local/system ASR, and optional CPU KWS. Device TTS had one active 24 kHz output and no recorder; device offline ASR had one active 16 kHz input and no TTS; stop returned both to zero. The cancel-before-start race was reproduced, fixed, and re-run as `capture_start_cancelled` with no recorder. | Proves process-local exclusion and terminal release for Xiaohei's wired paths. Cross-app exclusion, human audio, audible latency, routes, power, and public model rights remain open. |
| `VOICE-005` — audio-interruption policy | Partial OnePlus evidence | Static policy covers call/alarm/media/Activity and never resumes audio. A real local-ASR recording was started, backgrounded through Activity pause, then logged microphone release; post-stop AudioFlinger had no active record client and DSP remained active. | Genuine call, alarm, and media-focus runs; output-path/route behavior, unrelated apps, and human audio remain open. |
| `VOICE-010` — sentence-TTS queue | Passed on OnePlus 8T | Generation-bound ordered queue, explicit stale-callback invalidation, metadata-only queue logs, unit/static/signed-build gates; a real fixed-FAQ run created sequential 24 kHz tracks for sentences 1–4, while a second run stopped during sentence 2 and logged `dropped=3` with no later sequence submission. | Human intelligibility/naturalness, ≤300 ms audible interruption, barge-in/echo, routes, and long-duration power remain `VOICE-011/012` and human/physical gates. |
| `VOICE-011` — TTS interruption boundary | Automated/device boundary passed; human gate open | Visible Stop Speech invalidates the utterance/queue before engine stop; the OnePlus sentence-2 track was removed 227 ms after the user-control event, `dropped=3` and no later queue entry was submitted. `VOICE-004` provides process-local input/output exclusion. | Audio-track timing is not human audibility. A person must qualify Mandarin intelligibility/naturalness, ≤300 ms audible interruption, and an acceptable real acoustic echo-loop outcome before spoken barge-in is considered. |
| `CHAT-011` — deterministic offline FAQ | Complete in task scope | Five topics/25 exact aliases; ten unknown/action/injection and oversized rejection cases; fixed bilingual non-model label; zero added model/action/context use; static failed-only/no-network-thread boundary; fresh AOSP known match and unknown action denial stayed inside Conversation with zero fatal/ANR. | No 0.6B weights or generative local model are bundled. Broader offline inference, speech, and actions remain separate future gates. |
| `CHAT-012` — final Conversation acceptance | Automated scope passed; human gate open | Exact 20 questions, five interruptions, five timeouts, and five local privacy denials; five-category/15-phrase pre-request privacy policy; static zero-recorder API path; signed APK; fresh AOSP denial stayed in Conversation with zero fatal/ANR and zero active record clients. | `VERIFY`: no implemented Relay playback or human Mandarin intelligibility/natural-interruption run exists yet; automation cannot close that gate. |
| `ROUTE-002` — safe three-way handoff | AOSP user path passed | Reviewed `CommandRouter` plus imperative cues; exact 100-case synthetic matrix (40/35/25); conceptual command-keyword, ambiguous two-target, multi-step, and high-risk regressions; static/build gates pass. On fresh Android 14 ARM64 AOSP, a chat transcript opened Conversation with editable prefill and a zero-model/action status; a complex transcript opened Phone Agent with editable prefill and no planning/action; neither path auto-sent. | The handoff does not implement a remote planner, tool execution, generic agent, or user confirmation. Those remain separate gates. |
| `ROUTE-003` — zero-side-effect clarification | AOSP user path passed | Pure local target/intent/scope decisions; exact 50-case synthetic matrix with 30 asks and 20 clear controls; every ask is chat plus unknown command with zero guessed actions, model calls, or action calls. Fresh AOSP ambiguous text stayed on Home with `需要澄清；没有执行`. | No speech-acoustic qualification or follow-up-turn resolution was claimed. |
| `ROUTE-004` — unconfirmed ActionRequest | AOSP pending-planning boundary passed | Typed-user-only schema-v1 factory; immutable high-risk pending dry-run request; 39-case synthetic matrix including ten assistant confirmation forgeries; four public Schema fixtures; zero model/action calls; contradictory pending/live and pending/no-confirmation payloads rejected. Complex route handoff only prepopulates editable Phone Agent text; its Planning click constructs the typed request before private text can reach the configured planner, and binds any proposal to that same request. Fresh AOSP with the channel disabled visibly failed locally before any planner request. | A real remote planner call, confirmation-to-tool bridge, and executable complex plan remain separate gates. |
| `PLAN-001` — rules-first Task Plan v1 | Complete in task scope | Request-bound 1–8-step/1–60-second dry-run DAG; immutable step/dependency/argument views; exact 34-case Java matrix and five public fixtures; current seven-tool catalog/risk enforcement; forward DAG accepted; unknown tools, over-budget plans, malformed fields, and five real cycles rejected with zero model/action calls. | Intentionally not wired to any UI/model/executor; confirmation, catalog, authorization, and a bounded Android bridge now exist, while the minimal remote adapter in `PLAN-002` and device evidence remain separate gates. |
| `POLICY-002` — fresh scope-bound confirmation | Complete in task scope | Local-user-only, foreground/unlocked, memory-only one-use grant; task/request/plan and salted target/content digest binding; 1–60-second monotonic expiry; exact 50-case Java matrix and four public fixtures; target/content/identity change, expiry/rollback, lock/background, assistant source, cancellation, and replay fail closed with zero model/action calls. The only visible consumer is the cancel-first `TOOL-003` read-only test-collection query. | It remains unavailable to models, remote callers, arbitrary tools, or a generic executor. `UX-004` is still needed for a full proposal preview. |
| `TOOL-001` — versioned tool catalog | Complete in task scope | Immutable seven-entry v1 Android catalog; exact risk, ten real closed input/output schemas, same-catalog rollback target, audience, and 100–60000 ms timeout metadata; Java tests plus five public fixtures reject duplicates, unknown versions, missing schemas, unresolved rollback, unknown tools, and risk mismatch. Static gates prove zero model/platform/execution/persistence path and no new UI wiring. | Catalog membership grants no authority and rollback remains a declaration. `TOOL-002` supplies authorization and `TOOL-003` supplies a bounded Android bridge; visible confirmation and device acceptance remain before user-visible execution. OpenCode/root tools stay separate tiers. |
| `TOOL-002` — loopback capability authorization | Complete in task scope | One successful internal confirmation receipt exchanges once for a memory-only, `SecureRandom` 128-bit, 1–30-second token bound to confirmation/task/request/plan/call/tool/version/risk/audience/arguments/idempotency/time/privacy digest. Exact 50-group Java matrix plus seven public fixture files reject ten remote/cross-UID peers, confirmation reuse/scope, seven call drifts, three catalog drifts, five malformed/future/stale/private-metadata calls, expiry/clock rollback, replay/revocation, and foreign gateways. Active/replay state is bounded; model/action/execution calls stay zero. The local `TOOL-003` screen is its only visible consumer. | There is no socket listener or remote API. A trusted later transport must derive peer evidence rather than accept request JSON. |
| `TOOL-003` — bounded execution lifecycle | AOSP read-only path passed; real adapter gate open | Per-call timeout is capped by the reviewed catalog and bound into the capability digest; a private permit is consumed once; output is immutable/bounded/private. Exact 25-group Java coverage proves five injected successes, five interrupting deadlines, five cancellation paths, five typed failures, five denial/replay paths, nine acknowledged worker interrupts, and zero-or-one adapter calls. A closed Android registry and authorized bridge resolve only reviewed MediaStore-test/calendar-test adapters; global stop supplies the coordinator cancellation signal. On a fresh Android 14 ARM64 AOSP emulator, the user path from Home reached the hidden internal page, a prepare step executed nothing, the explicit local confirmation returned a real MediaStore test-collection count of `0`, then a second prepared request was cancelled; the debug APK was uninstalled and the emulator stopped. | `VERIFY`: this covers only the safe read-only query. Mutating adapter permission/rollback, trusted external peer, real network/process kill, physical-device and resource-teardown evidence remain open. |
| `OC-002` — OpenCode task protocol | Complete in task scope | A closed private `opencode-task.v1` proposal accepts only typed-user pending dry-run complex requests and exactly three reviewed kinds: project summary, test diagnosis, and controlled file organization. Exact Java coverage has three valid kinds, ten instruction-shaped attacks that remain inert, six source/kind/identity denials, immutable redaction metadata, and zero model/action/execution calls. Two valid/three rejected public fixtures and static no-UI/no-platform/no-generic-shell checks pass. | This is not an OpenCode runner: it creates no workspace, process, prompt, network connection, file access, or user-visible confirmation. `OC-003` must provide task-scoped paths and traversal/symlink/cross-task denial before any runner exists. |
| `OC-003` — task-private workspace boundary | Complete in task scope | A valid pending OpenCode task receives empty private `input`/`output` roots below a fixed app-private root. A real temporary-filesystem matrix proves two leases, four safe relative paths, seven absolute/traversal rejections, three symlink rejections, two cross-task rejections, duplicate-task denial, and zero content reads/writes/process calls. Path-free public lease fixtures and static no-runner/UI checks pass. | It is a resolver only: no OpenCode runner, file-content open, atomic safe open, user-visible confirmation, or cleanup lifecycle exists. `OC-004` and `OC-006` must add bounded execution and cleanup without reopening path authority. |
| `OC-004` — bounded OpenCode runner | Complete in task scope | An injected adapter receives only a valid pending task, matching private lease, bounded meter, and cancellation signal. The runner enforces reviewed profile/agent labels, 100–60,000 ms, 1–4,096 tokens, 1–32 steps, and 1–4,096 redacted output code points. Java coverage proves four successes, three limit breaches, five denials, one deadline and two cancellations; public budget fixtures and static process/network/content/UI gates pass. | It is not real `oc run`: it starts no process and connects no model/network, and no adapter yet proves meter completeness, safe file opening, cleanup or real-device cancellation. |
| `OC-005` — redacted visible progress | Complete in task scope | Six typed lifecycle events map to a public-log-safe read-only Agent card with only task kind, lifecycle stage, and bounded completed-step count. The matrix covers disconnected state, seven lifecycle transitions, terminal idempotence, and four sensitive-field exclusions; Android build and static UI/redaction gate pass. | The default card says disconnected because no live OpenCode adapter is wired. It is not evidence that OpenCode ran; OC-006 must provide stop/cleanup before real runner integration. |
| `OC-006` — stop and cleanup | Complete in task scope | A registered task stop cancels its worker, revokes active local gateway tokens, calls injected process/listener/tmux resource handles, and recursively deletes only its registered private lease without following symlinks. The temporary-filesystem matrix covers three resources, wrong-task denial, repeat-stop idempotence, recursive cleanup, and a preserved external symlink target. | Real OS process, port, listener and tmux handles are not started here. Actual adapters must terminate their own resources and pass independent device acceptance. |
| `OC-007` — restricted tools | Complete in task scope | A closed, zero-execution policy allows only project summary, test diagnosis, and controlled file organization. Its adversarial matrix permits three reviewed intents and rejects root, sensitive paths, destructive Git/delete, network, shell escape, and unknown text. | It is an intent boundary, not a shell parser or command executor. Future adapters must accept only approved typed operations and retain device acceptance gates. |
| `OC-008` — controlled-task matrix | Complete in task scope | Nine synthetic temporary-workspace rounds compose typed task protocol, restricted policy, injected bounded adapter, structured success and private cleanup: three each for summary, diagnosis and organization. Each kind also rejects a Git/network adversarial intent. | `real_opencode=0`: no user project, actual model, OpenCode process, network or real tool was opened. A real adapter/device acceptance remains required. |

## Evidence rules

- Source changes, unit tests, and static scans prove implementation only; they do not replace an environmental or user-facing acceptance run.
- A fixture, mock, or same-device profile-disabled run must be labeled as such and cannot satisfy its independent-account/device gate.
- A failure fingerprint is recorded once and addressed before a retry. Repeating screenshots, acoustic prompts, or paid-model calls without a changed condition is not evidence.
- Debug/test-signed APKs remain internal acceptance artifacts. They must not be presented as public releases.

## Authoritative records

- [M1 OnePlus record](acceptance-oneplus8t-m1.md)
- [M2 short commands](acceptance-m2.md)
- [M3 generic Android](acceptance-m3.md)
- [M4 notifications](acceptance-m4.md)
- [M5 Phone Agent](acceptance-m5.md)
- [M6 hardening](acceptance-m6.md)
- [M7 wake word](acceptance-m7.md)
- [CHAT-002 Keystore and token-free backup](acceptance-chat-002.md)
- [ROOT-010 independent lifecycle](acceptance-root-010.md)
- [REL-005 generic lifecycle recovery](acceptance-rel-005.md)
- [VOICE-001 offline Chinese TTS](acceptance-voice-001.md)
- [VOICE-002 offline system-TTS adapter](acceptance-voice-002.md)
- [VOICE-004 Android audio half-duplex](acceptance-voice-004.md)
- [VOICE-005 audio interruption](acceptance-voice-005.md)
- [VOICE-010 sentence-TTS queue](acceptance-voice-010.md)
- [VOICE-011 TTS interruption boundary](acceptance-voice-011.md)
- [Public release checklist](release-checklist.md)
