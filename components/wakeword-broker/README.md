# Wakeword Broker

Owns SoundTrigger lifecycle and emits `wakeword-event.v1`. The OnePlus 8T Qualcomm ADSP/LPI path is validated, but the production Android 14 broker and custom “Xiaohei” model remain future milestones.

负责 SoundTrigger 生命周期并输出 `wakeword-event.v1`。OnePlus 8T 的 Qualcomm ADSP/LPI 链已经实证，但正式 Android 14 Broker 与自定义“小黑”模型仍是后续里程碑。
