## Delivery task / 交付任务

Task ID / 任务 ID: `PROGRESS-001`

<!-- Keep exactly one Task ID field and replace the example with a stable ledger ID. -->
<!-- 只保留一个任务 ID 字段，并把示例替换为账本中的稳定 ID；依赖项可照常引用其他 ID。 -->

## Scope and dependencies / 范围与依赖

- Scope / 范围:
- Dependencies satisfied / 依赖已满足:
- Out of scope / 不在范围内:

## Acceptance and evidence / 验收与证据

- [ ] `STATUS.md` reflects Now, Next, blockers, and evidence.
- [ ] `docs/execution-backlog.md` and `docs/execution-backlog.zh-CN.md` agree.
- [ ] Acceptance command(s) and result(s) are listed below.
- [ ] Device, human, power, or offline-media gates remain `VERIFY` or `HUMAN` until actually completed.
- [ ] `docs/articles/`, models, APKs, credentials, and private raw evidence are not included.

Commands and results / 命令与结果:

```text

```

## Failure, rollback, and remaining gates / 失败、回滚与剩余门禁

- Root cause or failure fingerprint / 根因或失败指纹:
- Impact / 影响:
- Rollback / 回滚:
- Remaining gates / 剩余门禁:
